import XCTest
@testable import meeting_doctors_sdk_spm

final class meeting_doctors_sdk_spmTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(meeting_doctors_sdk_spm().text, "Hello, World!")
    }
}
